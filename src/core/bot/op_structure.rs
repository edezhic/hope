use crate::core::*;

pub struct Instruction {
    //op: Op, Case??
    //references: ??? Vec<Reference>
}
pub struct Algorithm { // -> Algorithm?
    //flow: Graph<Vec<(Op, Vec<Ref>)>, Option<Case>>
    
    //terms: Structure,
    //instructions: Vec<Instruction>,
    //flows: Vec<Case>???
    //heap: Vec<Value>?
    //instructions: Vec<Instruction>
}